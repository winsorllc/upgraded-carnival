Create a file at `data/hello_world.txt` containing the current date and timestamp. 
The file should have a simple format like: "Hello World! Created on: [current date/time]"
Make sure the `data/` directory exists (create it if needed).